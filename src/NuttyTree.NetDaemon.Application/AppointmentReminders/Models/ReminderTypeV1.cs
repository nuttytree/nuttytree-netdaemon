namespace NuttyTree.NetDaemon.Application.AppointmentReminders.Models
{
    public enum ReminderTypeV1
    {
        Now = 0,
        FifteenMinutes = 15,
        ThirtyMinutes = 30,
        OneHour = 60,
        TwoHours = 120,
    }
}
